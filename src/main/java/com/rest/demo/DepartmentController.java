package com.rest.demo;

import org.springframework.web.bind.annotation.RestController;

//A rest web service availavle in this endpoint that provides a list of countries
//http://localhost:8081/countries
//create restTemplate bean ->autowire it into the restController -> call the webservice using the getForObject method

@RestController
public class DepartmentController {

}
